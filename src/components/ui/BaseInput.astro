---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'input'> {
  class?: string;
}

const { class: className = '', ...rest } = Astro.props as Props;
---

<input class={`form-input ${className}`} {...rest} />
